# Dev Browser Authentication
# Copy this to your project's .env.local and fill in credentials

# Required: Login credentials
DEV_BROWSER_LOGIN_EMAIL=your-email@example.com
DEV_BROWSER_LOGIN_PASSWORD=your-password

# Optional: Auth type - "auto" (default), "clerk", or "standard"
# - auto: Automatically detects Clerk vs standard login pages
# - clerk: Force Clerk two-step flow (email → continue → password → sign in)
# - standard: Force single-page login (email + password + submit)
# DEV_BROWSER_AUTH_TYPE=auto

# Optional: Customize login page URL (default: http://localhost:3000/login)
# DEV_BROWSER_LOGIN_URL=http://localhost:3000/login

# Optional: Custom selectors (defaults work for most forms including Clerk)
# DEV_BROWSER_LOGIN_EMAIL_SELECTOR=input[type="email"], input[name="email"], input[name="username"], input[name="identifier"]
# DEV_BROWSER_LOGIN_PASSWORD_SELECTOR=input[type="password"]
# DEV_BROWSER_LOGIN_SUBMIT_SELECTOR=button[type="submit"]

# Optional: Wait conditions after login
# DEV_BROWSER_LOGIN_SUCCESS_URL=**/admin/**
# DEV_BROWSER_LOGIN_SUCCESS_SELECTOR=.logged-in-indicator

# ============================================================
# Clerk-Specific Setup (for Next.js + Clerk projects)
# ============================================================
# If using Clerk authentication, the skill auto-detects it.
# Just set the email/password and optionally the success URL:
#
# DEV_BROWSER_LOGIN_EMAIL=admin@yourproject.com
# DEV_BROWSER_LOGIN_PASSWORD=your-test-password
# DEV_BROWSER_LOGIN_SUCCESS_URL=**/admin/**
#
# The skill will handle Clerk's two-step flow automatically:
# 1. Enter email → Click "Continue"
# 2. Enter password → Click "Sign in"
# ============================================================
